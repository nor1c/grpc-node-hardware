syntax = "proto3";

package nor1c.hardware;

message Memory {
  enum Unit {
    UNKNOWN = 0;
    BIT = 1;
    BYTE = 2;
    KILOBYTE = 3;
    MEGABYTE = 4;
    GIGABYTE = 5;
  }

  uint64 value = 1;
  Unit unit = 2;
}